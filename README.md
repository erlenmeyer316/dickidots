# Roadmap

1. Add neovim setup to download and install latest version from neovim
2. Add dwm profile
   1. dwm setup script + config
   2. slstatus setup script + config
   3. dmenu setup script + config
   4. slock setup script + config
3. Add newsboat Rss Reader
